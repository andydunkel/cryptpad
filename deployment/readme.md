# Deployment information

## Windows

The native Lazarus Windows release is packaged with Inno Setup using
`setup_windows_laz.iss`.

## Linux

The native Lazarus Linux release is packaged as an AppImage. See
`linux/README.md` for build instructions and the release checklist.
